# includes/ klasörüne doğrudan web erişimini engelle.
# Bu dosyalar yalnızca PHP tarafından dahil edilir (require), tarayıcıdan açılmaz.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
